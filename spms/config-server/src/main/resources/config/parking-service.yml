server:
  port: 8082

spring:
  application:
    name: parking-service
  datasource:
    url: jdbc:postgresql://localhost:5432/spms_parking
    username: spms_admin
    password: spms_password

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
